---
interface Props {
  class?: string;
}

const { class: className = "" } = Astro.props;
---

<span class={`text-[9px] uppercase font-mono border border-indigo-500/20 text-indigo-500/80 px-2 py-0.5 bg-indigo-500/5 tracking-widest ${className}`}>
  <slot />
</span>
